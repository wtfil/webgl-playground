{
  "scripts": {
    "start": "webpack-dev-server",
    "build": "mkdir -p dist && cp -r public/* dist && webpack --mode production",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "html-webpack-plugin": "^3.2.0",
    "raw-loader": "^1.0.0",
    "ts-loader": "^5.3.3",
    "webpack": "^4.28.1",
    "webpack-cli": "^3.2.1",
    "webpack-dev-server": "3.7.2"
  },
  "dependencies": {
    "@types/events": "^3.0.0",
    "@types/gl-matrix": "^2.4.4",
    "events": "^3.0.0",
    "gl-matrix": "^3.0.0",
    "typescript": "^3.2.2"
  },
  "license": "MIT"
}
